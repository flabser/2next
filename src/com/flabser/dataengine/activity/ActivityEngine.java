package com.flabser.dataengine.activity;

public class ActivityEngine {
	/*
	 * ActivityEngine() { VoldemortConfig config =
	 * VoldemortConfig.loadFromEnvironmentVariable(); VoldemortServer server =
	 * new VoldemortServer(config); server.start(); }
	 */
}
