define('controllers/cost_center', [
    'ember'
], function(Ember) {
    "use strict";

    var Controller = Ember.Controller.extend({
        model: {
            name: 'hello'
        }
    });

    return Controller;
});
