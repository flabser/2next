define('routes/categories', [
    'ember',
    'routes/application'
], function(Ember, ApplicationRoute) {
    "use strict";

    var Route = ApplicationRoute.extend({

    });

    return Route;
});
