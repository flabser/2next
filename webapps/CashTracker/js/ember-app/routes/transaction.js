define('routes/transaction', [
    'ember',
    'routes/application'
], function(Ember, ApplicationRoute) {
    "use strict";

    var Route = ApplicationRoute.extend({

    });

    return Route;
});
