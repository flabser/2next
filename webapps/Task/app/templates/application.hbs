<div class="layout">
    {{partial 'header'}}
    <section class="main container">
        {{content-overlay action='hideOpenedNav'}}
        <section class="content">
            {{outlet}}
        </section>
    </section>
</div>
